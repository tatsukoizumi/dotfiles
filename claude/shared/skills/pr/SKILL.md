---
name: pr
description: リポジトリの規約に合わせて PR を作成する。承認前に gh pr create を実行しない。
---

# /pr

1. リポジトリの CLAUDE.md・PR テンプレート・既存 PR を確認し、規約（タイトル形式・言語・ラベル）に従う
2. ベースブランチを決め打ちしない。リポジトリの CLAUDE.md・既存 PR・デフォルトブランチ設定から判定する（develop や staging ベースの運用がある）
3. 現在のブランチが main（保護ブランチ）なら中断して報告する
4. 本文は writing ルールに従って書く: why 中心、変更ファイルの全件列挙をしない、数値で語る、前置きと締めの要約を書かない
5. タイトル・本文・ベースブランチをユーザーに提示し、承認を得てから `gh pr create` を実行する（承認前に実行しない）
